AutomationAnywhere Framework (Java 17 | Maven)

Run locally:
1. Install Java 17 and Maven.
2. Update src/main/resources/config.properties with credentials and file paths.
3. mvn clean test
4. Reports in reports/extent/ExtentReport.html ; HARs in reports/har/

CI:
Push to GitHub (main branch) and workflow will run tests.
